[package]
name = "hl-fuzzer"
version = "0.0.0"
authors = ["Hao Sun <sunhaoyl@outlook.com>"]
edition = "2018"
description = "System call fuzzer of healer"
license = "Apache-2.0"

[lib]
name = "hl_fuzzer"
path = "lib.rs"

[dependencies]
hlang = "0.0.0"
